#!/usr/bin/env python3
import os
import sys

sys.path.insert(0, os.path.realpath(os.path.join(os.path.dirname(__file__), '..')))

import darkrolls

if __name__ == '__main__':
    darkrolls.DarkRolls().run()
